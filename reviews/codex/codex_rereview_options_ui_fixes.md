Verdict: NEEDS CHANGES

I re-reviewed the fix pass for the seven findings in `codex_review_options_ui_and_completion.md`, with focused fan-out on atomic publishing, help-text accuracy, M7 source validation, M1 provenance, and null-note handling.

The fixes are real progress. The signal-history shrink guard now runs before live writes, history is staged with the option aliases, `option_candidate_delta` now matches the OTM-bucket logic, null notes are cleaned in the main paths, and the Tool A/C/D source anchors are mostly the right minimal columns. However, the pass is not fully closed yet.

| Severity | File:line | Finding | Why it matters | Fix |
| --- | --- | --- | --- | --- |
| HIGH | [golden_vector/common/files.py:125](C:/Users/Emanuel/code/Golden-Vector/golden_vector/common/files.py:125), [golden_vector/ingestion/persist_option_artifacts.py:131](C:/Users/Emanuel/code/Golden-Vector/golden_vector/ingestion/persist_option_artifacts.py:131), [golden_vector/cli.py:1998](C:/Users/Emanuel/code/Golden-Vector/golden_vector/cli.py:1998) | `atomic_write_many()` is transaction-like only for one publisher. There is no publish lock around the grouped option alias/history swap, and the standalone `option-artifacts` path does not appear to acquire the refresh lock. Two overlapping option publishes can interleave file swaps and leave a mixed alias/history set. | This keeps the original H2 class alive under concurrent CLI / background usage. The full refresh path has a lock, but the low-level publish primitive and standalone option-artifact command do not enforce single-writer ownership. | Acquire the same refresh/publish lock around `run_option_artifacts_outcome()` or around `publish_option_artifacts_and_history_atomically()`. Add a concurrency regression or a lock-behavior test. |
| MEDIUM | [golden_vector/common/files.py:100](C:/Users/Emanuel/code/Golden-Vector/golden_vector/common/files.py:100), [golden_vector/common/files.py:126](C:/Users/Emanuel/code/Golden-Vector/golden_vector/common/files.py:126), [golden_vector/common/files.py:135](C:/Users/Emanuel/code/Golden-Vector/golden_vector/common/files.py:135) | The `atomic_write_many()` docstring overstates the crash guarantee. A process kill during the forward swap loop can still leave a half-flipped alias set because rollback only runs on caught exceptions. A rollback restore can also fail after the Windows retry window and is swallowed. | For a local single-user tool this crash window may be acceptable, but the implementation should not claim true all-or-nothing under kill/crash. History is swapped last, so the most likely crash tear is old history plus mixed aliases, not advanced history plus mixed aliases. | Tighten the documentation and completion claims. If stronger crash safety is required, use a single manifest pointer or an explicit publish transaction file rather than N independent target swaps. |
| MEDIUM | [golden_vector/serve/candidate_finder_data.py:564](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/candidate_finder_data.py:564), [golden_vector/serve/candidate_finder_data.py:571](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/candidate_finder_data.py:571) | M7 still misses the required join key. A manifest-resolved Tool A/C/D artifact with the required ranking columns but no `ticker` passes `_read_optional_parquet()`, then `_prepare_source()` collapses it to an empty ticker-only frame. | That is the same readable-but-wrong artifact failure M7 was meant to stop: a current artifact can lose a whole dimension and still render instead of friendly-503ing. | Include `ticker` in the required-column validation for manifest-resolved current sources, or make `_prepare_source()` fail loud when a required current source lacks `ticker`. |
| MEDIUM | [golden_vector/hedge/option_artifact_frames.py:58](C:/Users/Emanuel/code/Golden-Vector/golden_vector/hedge/option_artifact_frames.py:58), [golden_vector/hedge/option_artifact_frames.py:69](C:/Users/Emanuel/code/Golden-Vector/golden_vector/hedge/option_artifact_frames.py:69), [golden_vector/hedge/option_artifact_frames.py:512](C:/Users/Emanuel/code/Golden-Vector/golden_vector/hedge/option_artifact_frames.py:512), [golden_vector/cli.py:1982](C:/Users/Emanuel/code/Golden-Vector/golden_vector/cli.py:1982) | The M1 provenance field can store the wrong kind of ID and hide mixed IDs. `tool_refresh_run_id()` says it returns the foundation refresh id, but falls back to `source_run_id`, which is a tool run id. It also takes the first non-null value, so a frame with multiple input refresh ids is persisted as one clean-looking id. | This is better than no provenance, but it is not a durable mixed-refresh contract yet. It can make a mixed input frame look aligned from the artifact alone. | Require exactly one `snapshot_refresh_run_id` for `built_from_tool_*_refresh_id`, or persist separate source-run fields. If multiple ids exist, preserve them as a delimited/list field or mark the provenance as mixed. |
| MEDIUM | [golden_vector/serve/column_help.py:199](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/column_help.py:199), [golden_vector/serve/column_help.py:983](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/column_help.py:983), [golden_vector/hedge/options_liquidity.py:589](C:/Users/Emanuel/code/Golden-Vector/golden_vector/hedge/options_liquidity.py:589), [golden_vector/hedge/options_liquidity.py:985](C:/Users/Emanuel/code/Golden-Vector/golden_vector/hedge/options_liquidity.py:985), [golden_vector/hedge/option_trading.py:534](C:/Users/Emanuel/code/Golden-Vector/golden_vector/hedge/option_trading.py:534) | `option_candidate_status` is closer, but still equates strict slot checks with displayed `Tradable`. The backend can accept a strict bucket candidate while preserving its real tier as `watch` because strict bucket caps can be wider than the global tradable spread. | A user can read the help as “strict slot pass means Tradable”, but the status badge is actually based on `candidate.liquidity_tier`. | Reword the help so strict/watch slot checks are described as candidate-selection gates, while the final displayed status is the real global liquidity tier of selected candidates. |
| MEDIUM | [tests/test_workspace_app.py:1966](C:/Users/Emanuel/code/Golden-Vector/tests/test_workspace_app.py:1966), [tests/test_workspace_app.py:2003](C:/Users/Emanuel/code/Golden-Vector/tests/test_workspace_app.py:2003), [golden_vector/serve/overview_tool_d.py:256](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/overview_tool_d.py:256), [golden_vector/serve/column_help.py:52](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/column_help.py:52) | The self-discovering `app_config` guard test misses `app_config=None`. It only checks whether the call text contains `"app_config"`, so `help_th(..., app_config=None)` passes. There is a live offender: Tool D flip table `Leverage @ G` uses threshold-backed `tool_d_leverage` with `app_config=None`, so the threshold sentence still drops. | The LOW finding was not fully fixed. The test gives false confidence that every threshold-backed help call receives live config. | Make the guard reject `app_config=None` for threshold-backed keys, and pass real `app_config` into the Tool D flip table render path. |
| LOW | [golden_vector/serve/detail_panels.py:303](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/detail_panels.py:303), [golden_vector/hedge/options_liquidity.py:724](C:/Users/Emanuel/code/Golden-Vector/golden_vector/hedge/options_liquidity.py:724) | The detail-page method text says Watch rows “passed one relaxed check.” The code requires all relaxed liquidity conditions together. | This is plain-English UI copy, but it still teaches the wrong rule. | Change to “passed the relaxed checks” or similar. |
| LOW | [config/candidate_finder.yaml:166](C:/Users/Emanuel/code/Golden-Vector/config/candidate_finder.yaml:166), [config/candidate_finder.yaml:182](C:/Users/Emanuel/code/Golden-Vector/config/candidate_finder.yaml:182), [golden_vector/serve/column_help.py:1071](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/column_help.py:1071), [golden_vector/serve/column_help.py:1144](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/column_help.py:1144) | Candidate Finder config labels still call Tool C/D 0-100 percentile scores “rank,” while the help layer was changed to reduce rank-vs-score confusion. | This reintroduces the language problem in presets and filter labels even if the column help is clearer. | Rename user-facing config labels/descriptions to “score” or “percentile score” where the value is 0-100, not an ordinal rank. |

Closed or acceptable:

- `option_candidate_delta` now correctly says OTM range assigns Near-ATM/Directional buckets and delta is a within-bucket fit/shape signal.
- Null-note handling is fixed in the main concise-note path. I did not find another concrete option-note leak after `_tuple_value()` and `collapsible_text_td()` were updated.
- The Tool A/C/D required-column anchors are mostly correct and minimal for Candidate Finder’s current dimensions. The missing `ticker` join key is the remaining hole.
- Additive provenance columns do not require a schema bump by themselves.
- Wiring the alignment engine to consume `built_from_tool_a_refresh_id` / `built_from_tool_b_refresh_id` can be deferred, but only after the provenance fields are made semantically precise.
- M5/M6 can remain deferred. The request-time scenario computation is still bounded enough for the local single-user tool.

Focused checks noted by sub-review: `tests/test_column_help.py`, the threshold guard test, and the strict-directional liquidity test were run by the help-review subagent and passed. I did not rerun the full suite.
