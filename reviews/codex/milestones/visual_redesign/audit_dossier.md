# Visual Redesign — Phase 0 read-only audit dossier (2026-08-10)

Starting commit 6c5015d on dev-vic (== origin/dev-vic; origin/main contains HEAD; origin/codex-source-mode fully merged; single worktree; tree clean except intentionally untracked naukri.md — PRESERVE, never commit/delete).

## Verified-exact plan claims (§3)
- workspace.css: 868 lines; 107 hex occurrences / 55 unique; 0 @media; 0 @import; two indentation regimes (1–657 indented 4sp, 659–868 top-level, boundary 657/659).
- Non-vendor color scan 192 occ / 69 unique = py(86/32) + js(1/1) + workspace.css(107/55) minus 2 HTML-entity false positives (charts.py:390, detail_panels.py:1036). Scope INCLUDES workspace.css.
- 24 HTML-emitting py files; 33 `<table`; 20 `<form`; 13 `<svg`. Zero `<caption`, zero `scope=`.
- .table-scroll used exactly 2× (candidate_finder_page.py:516,701; rule workspace.css:162). .flash-warning: 10 emit sites, 6 files, 0 CSS rules.
- --border/--paper referenced-undefined: bare at :556/:569 (discarded), with fallback #d9d2c4 at :791/:836. ALSO: --positive #2d6a4f / --negative #8a2d3b / --neutral #48566a defined-but-never-referenced; values re-hardcoded (charts.py:82).
- 9 flat nav tabs page_shell.py:8-18; `_page_shell(title, body, *, active_nav="")`; asset order: DT css → jquery/DT/4 first-party JS (defer) → workspace.css LAST.
- pytest --collect-only -q = exactly 1,555 (def count 1,433 across 136 files; 17 parametrize).
- Current theme is LIGHT cream/paper (--bg #f5f1e7, --panel #fffdf8, accent #b26700 ≈4.35:1 white-on-accent).
- aria-* in serve: 18 total (17 aria-label, 1 aria-expanded column_help.py:127). help_th called 203×, raw `<th>` only 17 — good centralization point for scope="col".
- Toolchain: Node v24.14.1 present; Python 3.14.3; no bs4/lxml/html5lib; no package.json/node_modules; ruff.toml only config (py311, select=["F"], line 100). NO pytest.ini/pyproject → no markers, CWD-dependent tests (guardrails use relative Path("golden_vector/serve/...")).

## Static server facts (http_helpers.py)
- GET-only /static/*; nested subdirs work (plain Path join + resolve + relative_to root check); allowlist EXACTLY {.js, .css} (:17-20); vendor/ → immutable 1y, else no-cache (:168-172, checked on resolved rel path); no ETag/Last-Modified/conditional handling; OSError→404 bodyless (_static_not_found :175-177).
- /static/css/tokens.css would serve today with correct MIME, zero code change; css/ dir doesn't exist yet. @import relative or root-absolute both resolve.
- SPLIT BLOCKERS: test_workspace_datatables.py:114-125 asserts Cache-Control no-cache AND ":root" AND ".top-nav" in raw workspace.css body; :127-135 same :root via vendor/.. path. test_workspace_app.py:2492-2494 scans only workspace.css for .help-term/th[title].

## Route inventory (workspace.py linear if-chain :153-764)
Order: /static/* (GET) :158 → /favicon.ico GET→204 CL:0 :161 → /hedge-readiness GET→303 /option-trading :164 → /hedge-readiness/latest.md GET (403 disabled → download md / bodyless 404) :167-182 → /portfolio GET :184 → /portfolio/reconciliation.csv GET :205-221 → /portfolio/lots* prefix ANY (403-if-disabled BEFORE method check :224-232; non-POST/unknown → 404 "Unsupported portfolio route." :233-238,:259-264; add :240; {id}/edit :248; {id}/delete :256; errors PortfolioError/FileNotFoundError/ValueError → 400 re-render :269-278; success 303 /portfolio?saved=portfolio :279) → /refresh + /option-trading/refresh POST-only :281-291 → / GET :293 → /tool-a :314 → /tool-b :329 (ScreeningOverrideError → 400 FULL page re-render w/ override_error, overrides=ScreeningOverrides() :340-355) → /tool-c :370 → /lab :385 → /lab/dial/* :406 (extra unquote() double-decode :407; scenario default "gold_down", benchmark default "GDX" upper :409-410) → /scorecard :428 → /tool-d :434 → /option-trading :450 → /candidate-finder :467 (byte-identical to / except base_path) → /ticker/* ANY :488-758 → fallback 404 "Page not found." :760-764.
- NO 405 anywhere; NO HEAD support (HEAD → 404 HTML). Method mismatch → 404 (two flavors: generic + "Unsupported ... route.").
- 500: bare except :813-821 friendly page. 503 typed: OptionArtifactStaleSchema :765, ToolBStaleSchema :777, PortfolioStaleSchema :789, CandidateFinderSource :801.
- ALL error pages set active_nav="candidate_finder" (http_helpers.py:37-43) — CF highlighted on every error page (fix presentationally: no active nav on error shell).
- Downloads: reconciliation.csv text/csv attachment filename golden-vector-portfolio-reconciliation.csv no-cache :216-221; missing MANIFEST KEY → PortfolioStaleSchemaError → 503 (:826-837); only unreadable file → bodyless 404. latest.md text/markdown attachment; missing file → bodyless 404; NO UI link to either hedge-readiness route (URL-only legacy).
- POST /option-trading/refresh is DEAD (render_option_refresh_control called only from candidate_finder_page.py:132 with default action="/refresh"). Route must stay; no UI adds it (§15.6).
- Refresh POST: start_options_refresh result DISCARDED :283; double-POST while running → same 303, no signal; spawns detached `python -m golden_vector.serve.option_refresh --run` (windowless).
- _safe_return_to :840-844 rejects empty/non-"/"-leading/"//"/"\\" but NOT CR/LF → Location header injection (loopback-only, low sev, pre-existing).
- Unknown ticker + lens=option-trading loads option dataset BEFORE 404 decision :498-513 → can 503 for bogus ticker.
- /ticker/ empty → 404 " is not an active Corporate Finance ticker." (workspace_state.py:408-414 _parse_ticker_route).
- Combined: no handler; fallback 404; test test_workspace_app.py:2966.

## Query/form nuances vs §6.1
- CF _hidden_query_inputs (candidate_finder_page.py:809-823) round-trips ALL unknown query params through all 3 GET forms (repeated values incl.). `saved` never read on CF (no flash — dead-end after ticker-POST return_to=/). Preset links carry only preset/gold_price/fundamentals_source/beta_window → intentionally reset builder state.
- Ticker return_to = ALL first-value params minus lens-unless-option-trading (workspace.py:112-121), saved popped detail_page.py:68.
- Tool B: fundamentals_source else rank_by fallback :333-336; rank_by read-never-rewritten (one-round-trip degrade; _add_view_params overview_tool_b.py:829-838 always emits fundamentals_source). differences_only truthy set {"1","true","yes","on"} :859-863.
- Tool D form has NO hidden inputs (overview_tool_d.py:273-292); presets/reset carry only gold_price/search/fundamentals_source. Yahoo compute failure → silently resets to finance_source="our" while URL says yahoo (:90-95).
- beta_window invalid → silent blend fallback (candidate_finder_data.py:262). /lab horizon int() silent fallback; bucket requested→default→first (workspace.py:387-397). option_horizon accepts MOST_LIQUID_HORIZON sentinel.
- Option sizing GET form (detail_panels.py:1102-1126) → action /ticker/{T}#option-sizing, hidden lens + conditional fundamentals_source=yahoo, does NOT carry window (drops active ?window=).
- Forms: CF 4 (3 GET + refresh POST w/ return_to=base_path — confirmed base-path-only, candidate_finder_page.py:132-135, option_refresh.py:446-462); Tool B 3 GET (advanced `<details>` open when overrides.has_non_gold() :665; hidden via _render_overrides_as_hidden_inputs :800-817); Tool A/C 1 each (visible search, hidden window; window tabs = links carrying window+search only); OT 1 (option_horizon; NO refresh button on page — confirmed); Lab 1 (horizon,bucket); Scorecard 0; Ticker company/reporting/verification(×N per REQUIRED_MANUAL_FIELDS, hidden field_name, disabled "Choose status" placeholder)/note + return_to hidden (detail_forms.py:360-364); Portfolio add + per-lot edit + per-lot delete (delete: no inputs, no confirm, lacks .portfolio-lot-form class deliberately), payload keys portfolio_form_payload :66-74, NO return_to, always 303 /portfolio?saved=portfolio. Portfolio "17 tables/25 forms/22 disclosures" are DATA-DEPENDENT (1+2×lots), not constants.

## Confirmed behavioral defects (pre-existing; Q3 decision)
1. 4 ticker-POST 400 branches (workspace.py:615-628 company, :652-665 reporting, :703-716 verification, :735-748 note): render_detail_page WITHOUT active_window/canonical_anchor/lens/app_config/financials_source/query_params/option_trading_detail/provenance/manifest → defaults active_window="12M", canonical_anchor="12M" HARDCODED (wrong anchor marker), lens tool-a, source "our", return_to collapses to bare /ticker/{T}; error uses `<div class="flash">` = SUCCESS class (detail_page.py:111-112); app_config=None strips all column-help on 400 page.
2. Empty company POST (nothing typed/ticked) → 303 saved=company "Company inputs saved." no-op save (workspace.py:609-613).
3. Sizing form drops window (above).
4. _safe_return_to CR/LF gap.
5. Unknown-ticker+option-lens 503-before-404.
6. CF parse failure (?gold_price=abc/0/-1/inf) → BARE 400 error page (workspace.py:297-302,:471-476) vs Tool D warm 200+warning vs Tool B 400 full-page — §15.1 wording half-true (runtime failure falls back with warning candidate_finder_page.py:280-281; parse failure does not).
7. /lab/dial double-unquote asymmetry.

## Blank-handling contracts CONFIRMED
Company: clear_{field} first → None+continue; blank → continue (workspace.py:596-608). Reporting: all 3 fields always sent; blank → None = clear (_coerce_form_text, workspace.py:636-646, format_helpers.py:279-281). Verification: clear branch first (wins) then blank no-op (workspace.py:684-695); field_name must be in REQUIRED_MANUAL_FIELDS else 400; status .strip().upper() unvalidated at route.

## Tests audit
- §18.1 17 files exist; counts: workspace_app 82, datatables 28, candidate_finder_page 20, horizon_switcher 28, option_routes 19, option_overview 8, lab_page 13, lab_curve 55, lab_scorecard 12, column_help 20, metric_formula 14, url_helpers 6, explanations 4, fund_provenance 2, model_state 20, option_refresh 16, portfolio_m1 34. 35 files import serve total; plan omits e.g. benchmark_comparison 28, candidate_finder_data 38, screening_overrides 17, windows 6+6, rebased_overlay_panel 16, detail_volatility_context, option_signal_charts, header_context 10, lab_* family.
- Guardrail scans: tool_d :2351, tool_b :2373, metric_formula :2414, dual-source :2438 (detail_panels+format_helpers), charts :2458, info-affordance rglob serve/**+js+css :2479, CF forked-math :2564 (fillna ALLOWED there), GLOBAL :2978 = glob("*.py") NON-RECURSIVE (allowlist detail_panels np.polyfit/.std; candidate_finder_data .fillna) — new serve/ subpackage ESCAPES; lab :291 (~55 tokens incl. "Defensive"/"Pro-cyclical"/"Steady" literals, / len(), np.*), scorecard :367, benchmark_comparison :391-401 (charts+detail_panels+beta strip), rebased_overlay :168 (overlay-crosshair.js tokens). All relative-path CWD-dependent.
- Gaps ALL CONFIRMED: no /scorecard request anywhere; no reporting POST route test (store-level only test_manual_data.py:143); no portfolio edit/delete route tests (store-level test_portfolio_m1.py:53; 400 branch untested); zero 405/PUT/DELETE/PATCH/HEAD tests; no route matrix (near-miss :2119 tool-c windows); no CSS token/color tests (test_lab_curve.py:624 asserts stroke="#2f6f6d" — WILL break on chart tokenization, update intentionally); no a11y tests (3 incidental aria-label asserts); no HTML parser use anywhere (all substring).
- Node runtime test: ONLY test_rebased_overlay_panel.py:178 (overlay-crosshair) via subprocess node -e + vm.runInNewContext + ~20-line Element shim; NO jsdom; NO skip guard (node missing → FileNotFoundError ERROR); cwd-dependent. Extending to DataTables infeasible (vendored DT = jQuery build; jQuery needs real DOM). Extends cheaply to help-popover.js + rug-tooltip.js (no jQuery).
- WSGI helpers: FIVE near-duplicates — canonical test_workspace_app.py:1173 _call_wsgi_app (imported by lab_page:234,270 + horizon_switcher:33-34); option_routes:531; option_refresh:393; candidate_finder_page:463 (NO body); datatables:37 _call_wsgi_raw (bytes); portfolio_m1:1809 _call_wsgi (hardcodes QUERY_STRING "", no CONTENT_TYPE). Consolidate into tests/helpers.py for matrix work.
- conftest.py: single autouse _block_external_network (socket monkeypatch, loopback-only) — guarantees no-external-requests in tests. tests/helpers.py:8 build_test_paths copies config/ EXCLUDING portfolio.local.yaml (privacy). Fixtures: tests/fixtures/lab/dial_golden_13w.parquet, options/aem_chain_20260529.parquet; regen tests/tools/regen_dial_golden.py.
- Privacy: test_portfolio_m1.py:398 (403s + loopback ValueError + SECRET HOLDINGS), :444 gitignore paths, :452 broker-account regex over tracked files. Static security: datatables :91-176 (8 tests = whole surface). safe_return: only 2 route tests (option_refresh :289,:312; positives :252,:281). 500 redaction :2947. Combined removed :2966.

## JS contracts
- workspace-tables.js 94 lines: guard :40 typeof window.DataTable → return (no-JS OK); table.js-datatable :42 (8 pages × 1 table); config paging:false info:false order:[] :49-58; columns from th.dataset.colName + data-sort-numeric→type num; filter bar match el.dataset.filterTarget === '#'+table.id :64-68; global search input[data-global-search] :71-76 (NOTE: data-search is a DIFFERENT contract — DataTables cell filter key emitted column_help.py:208); column filters select[data-filter-column] → dt.column(name+':name').search('^esc$',true,false) :79-87; zero innerHTML.
- help-popover.js 156: .help-icon via closest() capture-phase doc click + stopPropagation :124-145 (sort-conflict protection); singleton div.help-panel role=dialog aria-label "Column explanation" tabindex -1; content textContent-only from data-help-*; read-more :81-99; clamp left/right + flip-above-bottom :103-122 (margin 8; no top-overflow handling beyond max(margin), fixed max-width 340px css:692); dismiss outside/Escape+focus-return/scroll(capture)/resize; aria-expanded maintained; panel.focus() on open.
- rug-tooltip.js 65: div.rug-tooltip role=status; .rug-tick[data-rug]; strips server <title> when JS live :16-18 (no-JS fallback keeps title); naive x+14/y+14 NO clamping; dismiss mousemove-off/mouseout/doc-mouseleave/blur; NO Escape/scroll/resize; textContent-only; hover-only (no touch/focus).
- overlay-crosshair.js 155: svg.overlay-chart; data-overlay JSON from charts.py:442/:455 (pct pre-formatted server-side); hardcoded stroke #6f685c :35 (dup of --muted); dot fills server s.color; clamps ALL 4 edges :58-68; dismiss svg-mouseleave/doc-mouseleave/blur/scroll(capture); NO Escape/resize; ONE innerHTML sink :116 fully esc()'d (incl. ' in style attr) :12-19,:101,:109-110.
- Z-INDEX INVERSION: .help-panel (dialog) z=1000 (css:690) UNDER .rug-tooltip z=9999 (:338) + .overlay-tooltip z=9999 (:354). Only 3 z-index decls total.
- Inline script: ONLY portfolio_page.py:653-668 _portfolio_currency_script (emitted :474 empty-path or :496; plain literal no f-string); .portfolio-lot-form = add :462 + edits :481 (NOT delete :490); reads select[name=ticker]/[name=buy_currency]; option data-currency :538 from ticker_info currency escaped; change+load sync non-empty only. Extraction to static/portfolio-forms.js safe with defer (NOT bare head script).

## Primitives
- _metric_card: format_helpers.py:78-83 (title,value; 26 sites: CF 5, detail_panels 18, overview_helpers 3) vs portfolio_page.py:562-564 (+keyword help_text → column_help.help_term; 8 sites, 6 with help; NO key= registry support). Reconcile: add keyword-only help_text (+key) to format_helpers version; no circular import (column_help imports only contracts.config_models).
- windows.render_window_selector windows.py:137-157 (STRUCTURAL_WINDOWS; carries search; panel window-switcher / window-tabs / window-tab active). win_num_td :111, gold_link_td :126 emit data-order.
- model_state_banner: render_model_state_banner :51-69 (renders NOTHING when state=="complete"); render_option_freshness_box :13-48 (ok hint / stale flash; 3 label variants).
- overview_helpers: _render_refresh_summary :83-100 (3 _metric_cards or empty-manifest panel); _render_filter_bar :127-159 (section.panel.table-filters data-filter-target → label.filter-global input[data-global-search] + label.filter-column select[data-filter-column]; EXACT js contract); _collect_filter_options :103-124; _render_provenance_warnings :27-80 (deliberately UNESCAPED <code> literals :32-39 + escaped run-ids :69 — preserve escape split).
- column_help.py: help_th :137-167 (203 sites; label,key,app_config,col_name,sort_numeric,text,panel); help_icon :106-134 (button.help-icon aria-expanded data-help-*); help_term :88-103; help_value :195; VALUE_HELP :174; COLUMN_HELP :324; data-search cell key :208.
- metric_formula.py: _METRIC_FORMULAS :47; metric_formula_icon :186 → delegates help_icon (single path).
- url_helpers.build_page_url :18-41 ((path,current,set_params); None deletes; RAW url — callers escape; ratified reviews/codex/AGENT_SYNC.md).

## CLI / server
`python main.py workspace [--host 127.0.0.1] [--port 8765]` (cli.py:590-604, dispatch :734-735; run_workspace :2740-2765). wsgiref simple_server single-threaded :883. Loopback guard when portfolio enabled :874-877 (_is_loopback_host :893-900). Startup: config load + ticker filter + manual_store_exists check only — NO network/refresh; artifacts lazy per request. Only network path = explicit POST /refresh (spawns detached option_refresh --run). NEVER click live Refresh during QA.

## Planned focused release selection (to pin in Phase 0)
File-path-based (no markers exist). Union: the 17 §18.1 files + test_benchmark_comparison, test_rebased_overlay_panel, test_screening_overrides, test_windows, test_windows_registry, test_candidate_finder_data, test_header_context, test_detail_volatility_context, test_option_signal_charts, test_option_trading_detail_panel, test_lab_behaviour_panel, test_lab_gold_profile, test_lab_validation, remaining test_portfolio_* (6 more), test_option_carry_forward, test_option_horizon_selection, test_option_trading_data, test_cli_workspace + new redesign test files. Always run from repo root (CWD-dependent).

## Corrections to apply to plan (after Codex answers) — abbreviated list
§3 note 192/69 scope; §6: no-405/no-HEAD reality, two 404 flavors, lots 403-before-method, reconciliation 503-vs-404, latest.md bodyless 404 + no UI links, dead /option-trading/refresh, error-nav CF highlight (presentational fix: none), favicon no <link>; §6.1: CF arbitrary-param round-trip, saved dead-end, return_to all-params rule, rank_by degrade, differences_only truthy set, Tool D no-hidden-inputs + silent source reset, beta_window/lab silent fallbacks, sizing form drops window, preset reset semantics; §12.1 add empty-company-save nit + 4-branch detail (flash class, app_config, anchor); §15.1 parse-vs-runtime failure split; §15.7 counts data-dependent; §17 Phase 0 add: pin focused selection command, rglob scan fix, consolidate WSGI helper, node skip guard (per Q2), test_lab_curve color assertion note; §18.1 add missing files incl. benchmark_comparison + rebased_overlay guardrails; §18.2: matrix pins 404-not-405 + HEAD; §16 add portfolio-forms.js exists-not; z-index scale documents inversion fix; §9.2 note 3 dead tokens; Phase 1 defines --border/--paper.

## The 5 Codex questions (sent 2026-08-10)
Q1 evidence storage/git policy → rec (c) commit text + curated sanitized shots, full matrix untracked.
Q2 DataTables runtime testing → rec (b) shim for popover/rug + node --check + browser-matrix evidence for DataTables; add node skip guard.
Q3 behavioral defects → rec (a) strict defer to follow-up milestone; pin current behavior with known-defect comments.
Q4 Phase 3 gate → rec (b) autonomous continue with evidence pack.
Q5 CSS @import split → rec (a) proceed, update 2 tests intentionally, measure; fallback (c) multiple <link> if timing regresses.
