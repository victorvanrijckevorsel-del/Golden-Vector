# Response to the independent final review (GV-RD-FINAL-001..012)

- **Date:** 2026-08-10 · **Branch:** `dev-vic`
- **Review:** `reviews/codex/2026-08-10_visual_redesign_final_review.md` (0 HIGH, 4 MEDIUM, 8 LOW)
- **My verdict on the review:** all 12 findings accepted as real. On three I chose a different remedy than the review implied (002, 004, 007/011) — reasons below. Everything is fixed in `ed756a9` (code) + the follow-up matrix-fix commit; evidence in `browser_evidence_final_matrix.json`.

| Finding | Sev | Resolution |
|---|---|---|
| FINAL-001 D1 context loss | MED | All four validation-error branches re-render through ONE helper that resolves window/anchor/lens/source/query exactly like the GET path — from the validated `return_to` (what real bare-action forms actually send) overlaid by any POST-URL query — and echo submitted values via a new `form_overrides` slot (company/reporting/verification; the add-note form renders no values, so nothing to echo). Regressions include the bare-URL shape. Register D1 entry annotated. |
| FINAL-002 pointer-only charts | MED | **Different remedy:** accessible equivalent instead of keyboard-interactive SVG. Every pointer-revealed value (overlay crosshair, rug ticks) is also a collapsed "Chart data (table)" disclosure in a labelled region, built from the exact embedded payload (zero new math). Equivalence pinned by `tests/test_chart_data_tables.py` (rows == payload entries; spot values match). Phase 6 evidence corrected: the "pointer events cover touch" overclaim is retracted in place, gap marked resolved. |
| FINAL-003 sticky nav self-occlusion | MED | Sticky geometry single-sourced in tokens; **below the desktop breakpoint the section-nav is static** (pills wrap to content-dependent rows — no fixed token can size a stuck multi-row nav), and root `scroll-padding-top` clears the sticky header. Live proof at 390px: target lands at 68px vs header bottom 61px, visible. |
| FINAL-004 matrix smaller than blueprint | MED | **Different remedy detail:** one consolidated scripted pass with NAMED widths 1440/1280/1024/768/640/390/320. Zoom via WCAG 1.4.10 reflow equivalence (640px == 200%, 320px == 400% at 1280 base) instead of per-route zoom emulation. Error states, anchor visibility, tap targets, first-focus, forms, back/forward all recorded. **The restored gate immediately caught three real defects** (20rem grid columns overflowing at 320px, 16px help-icon tap targets, the nav-wrap occlusion) — all fixed and re-verified: zero violations across all 77 loads. |
| FINAL-005 D9 silent on main screen | LOW | Candidate Finder renders the same already-running info notice as the option overview; route tests for `/` and `/candidate-finder`; live-verified in the matrix. |
| FINAL-006 weak defect regressions | LOW | D2 now byte-compares the manual store before/after the all-blank POST. D5 uses an on-disk stale-artifact fixture proving unknown-ticker 404 precedes option loading, with a benchmark control proving the 404 comes from membership, not the stale data. D7 got a route-level failing-Yahoo test asserting the warning names both sources. |
| FINAL-007 purity guard overclaim | LOW | **Strengthen + narrow:** new AST ratchet forbids arithmetic operators, ordering comparisons, numeric builtins, and numeric-literal addition in `serve/ui` (green with zero violations at introduction); README now states exactly what is machine-enforced vs review-ruled. |
| FINAL-008 `.table-scroll` orphan | LOW | Wrapper replaced with `table_region` per criteria group (labelled, keyboard-reachable); dead-selector guard now matches word-boundary tokens and its docstring claims only what it checks. |
| FINAL-009 overflow-guard bypasses | LOW | Guard checks every comma-separated selector arm and the `overflow` shorthand (auto/scroll values). |
| FINAL-010 DataTable guard gaps | LOW | `/tool-c` added; emitter-module inventory test fails on any `js-datatable` emitter without a covered route; docstring states true coverage. |
| FINAL-011 contrast sample vs claim | LOW | **Strengthen + narrow:** a self-maintaining scan finds every first-party CSS block declaring its own color + var() background and requires AA 4.5:1 or a reasoned exemption (zero exemptions needed today — all ~40 blocks pass). Inherited/composited combinations are explicitly browser-gate territory, and the handoff wording now says so. |
| FINAL-012 Portfolio nav vs §15.7 | LOW | Blueprint subsection map implemented: distinct `#gold-beta` and `#currency` anchors, one `#lots` region spanning Add + Edit, nav renamed "Lots", full §15.7 order kept alongside the Summary/Data-issues extras. |

**Verification:** targeted suites green throughout; consolidated matrix all-clean (`browser_evidence_final_matrix.json`); full focused selection re-run green on the final tree (count in the handoff).

**QA infrastructure note (no production change):** the browser gate wedged single-threaded `wsgiref` via Chrome's speculative preconnect sockets (accept + silent recv blocks forever). Gates now launch the server through `.playwright-mcp/serve_with_socket_timeout.py` (5s socket default timeout, gitignored).
