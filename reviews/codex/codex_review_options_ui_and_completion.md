Verdict: READY WITH CHANGES

I reviewed the `dev-vic` diff first-hand against `main`, with three parallel sub-reviews focused on option publish/freshness, the click-to-explain rollout, and the concise-notes/helper paths. The candidate-backed most-liquid default, the manifest-resolved option reads, the M4 `MISALIGNED` freshness state, and the unreadable-current-artifact -> friendly 503 path are all real improvements. The remaining issues are below.

| Severity | File:line | Finding | Why it matters | Fix |
| --- | --- | --- | --- | --- |
| HIGH | [golden_vector/cli.py:1985](C:/Users/Emanuel/code/Golden-Vector/golden_vector/cli.py:1985), [golden_vector/hedge/option_signals.py:168](C:/Users/Emanuel/code/Golden-Vector/golden_vector/hedge/option_signals.py:168), [golden_vector/ingestion/persist_option_artifacts.py:68](C:/Users/Emanuel/code/Golden-Vector/golden_vector/ingestion/persist_option_artifacts.py:68) | H2 is improved but still not truly all-or-nothing. The build stages run-stamped frames, then mutates `option_signal_history.parquet`, then flips `*_latest` aliases one file at a time. If alias promotion fails mid-loop, the run has already advanced the only accumulated IV-rank history, and any aliases written earlier stay advanced. | That leaks unpublished state into later signal builds and can still leave a mixed option alias set, which is exactly the class of state-torn publish H2 was meant to eliminate. | Make the option publish atomic across both the history update and alias promotion, or move both under one manifest-swapped current-state pointer instead of file-by-file alias writes. |
| MEDIUM | [golden_vector/serve/column_help.py:975](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/column_help.py:975), [golden_vector/hedge/options_liquidity.py:716](C:/Users/Emanuel/code/Golden-Vector/golden_vector/hedge/options_liquidity.py:716), [golden_vector/hedge/options_liquidity.py:738](C:/Users/Emanuel/code/Golden-Vector/golden_vector/hedge/options_liquidity.py:738), [golden_vector/hedge/option_trading.py:526](C:/Users/Emanuel/code/Golden-Vector/golden_vector/hedge/option_trading.py:526) | `option_candidate_status` help text describes the wrong rule. The text points to broad spread bands, but the actual status badges come from bucket-specific slot checks that combine bucket fit, spread, open interest, minimum mid, and IV usability. | The new `ⓘ` system’s main risk is telling the user a plausible but false decision rule. This entry currently does that. | Rewrite the help text so it matches the slot-level liquidity contract, or factor the slot rule into a single shared explainer primitive that both code and help text consume. |
| MEDIUM | [golden_vector/serve/column_help.py:1353](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/column_help.py:1353), [golden_vector/hedge/options_liquidity.py:834](C:/Users/Emanuel/code/Golden-Vector/golden_vector/hedge/options_liquidity.py:834), [golden_vector/hedge/options_liquidity.py:865](C:/Users/Emanuel/code/Golden-Vector/golden_vector/hedge/options_liquidity.py:865), [golden_vector/serve/detail_panels.py:310](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/detail_panels.py:310) | `option_candidate_delta` says delta is “the basis for the strike buckets”. It is not. Bucket membership is decided by OTM range; delta is only a later fit score inside an already-chosen bucket. | This is a direct accuracy error in the new explanation layer, and it conflicts with the page’s own glossary. | Change the meaning text so delta is described as a fit/shape signal, not the bucket selector. |
| MEDIUM | [golden_vector/serve/candidate_finder_data.py:559](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/candidate_finder_data.py:559), [golden_vector/serve/candidate_finder_data.py:585](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/candidate_finder_data.py:585), [golden_vector/serve/candidate_finder_data.py:954](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/candidate_finder_data.py:954) | M7 only fail-louds unreadable current artifacts. Readable-but-wrong manifest-resolved Tool A/C/D parquets still flow through `_prepare_source()`, can collapse to a ticker-only frame, and then only generate warnings. | That still allows a 200 Candidate Finder page with whole dimensions silently absent, which is the same failure class M7 was supposed to close for current-state sources. | Add schema/required-column validation for manifest-resolved Tool A/C/D sources, not just Tool B, and route current-state mismatches to the same friendly 503 path. |
| MEDIUM | [golden_vector/serve/format_helpers.py:149](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/format_helpers.py:149), [golden_vector/hedge/option_artifact_frames.py:630](C:/Users/Emanuel/code/Golden-Vector/golden_vector/hedge/option_artifact_frames.py:630), [tests/test_common_helpers.py:119](C:/Users/Emanuel/code/Golden-Vector/tests/test_common_helpers.py:119) | `collapsible_text_td()` handles top-level missing values, but not missing members inside lists/tuples. `None` becomes `"None"` and `pd.NA` becomes `"<NA>"`. The option-artifact note tuple loader also stringifies `null` members before the helper ever sees them. | This is user-visible garbage in the new concise-notes path, and it breaks the helper’s contract on exactly the surfaces it was introduced to clean up. | Drop null-like members before stringification in both the tuple loader and `collapsible_text_td()`, and add tests for mixed lists like `[pd.NA, "ok"]` and JSON-null note payloads. |
| LOW | [golden_vector/serve/column_help.py:52](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/column_help.py:52), [golden_vector/serve/detail_page.py:108](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/detail_page.py:108), [golden_vector/serve/detail_panels.py:241](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/detail_panels.py:241), [golden_vector/serve/overview_tool_d.py:253](C:/Users/Emanuel/code/Golden-Vector/golden_vector/serve/overview_tool_d.py:253) | The “site-wide live thresholds from the central registry” claim is overstated. Some render paths call `help_th(...)` without `app_config`, so threshold-backed help silently loses its config-resolved threshold sentence. There are also still plain `<th>` headers outside the registry sweep. | This is not a model bug, but it means the rollout is incomplete and future readers will assume those explanations are fully live when they are not. | Pass `app_config` through every threshold-backed help call and add a render-level test that every thresholded header path actually receives it. |

Deferred-item calls:

- M1 mixed-refresh warning-only: I do not think the current warning-only shape is sound. The option artifact build still reads mutable latest Tool A/Tool B/options inputs, while the published option artifacts do not durably stamp the Tool A/Tool B refresh ids they were built from. That makes the warning reconstructed state, not auditable provenance. This should become either a hard block or a persisted provenance contract.
- M5/M6 precomputing scenarios / persisting scenario ladders: acceptable to defer for now. The current request-time compute is bounded and local-user scale.
- `cost_curve_aisc_percentile`: I do not see a current model bug. The displayed raw percentile is intentionally “low is cheaper,” and the resilience component then inverts it through `quality_components["cost_curve_aisc_percentile"] == "low_good"`. The risk here is readability and future drift, not wrong current math.
